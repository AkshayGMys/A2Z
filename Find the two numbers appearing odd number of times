
NOT ABLE TO GET THE LOGIC

PROPERTY use 
1) a ^ a = 0
2) if a = 100010101000 then a-1 = 100010100111. NOte that rightmost 1 is 0 and all the 0's to the right of 1 become 1.
3) (a ^ (a-1)) ^ a = 0000000100 . This means only  the right most set bit (bit that is 1 ) is given in the result.


//Back-end complete function Template for C++
class Solution {
  public:
    // Function to find two odd occurring numbers in an array.
    vector<int> twoOddNum(vector<int>& arr) 
    {
        //Creating two buckets 
        vector<int> output;
        int b1 = 0;
        int b2 = 0;
        
        //Xor ing all numbers
        int res = 0;
        for(auto &it : arr)
            res^=it;
        
        //Getting only the first bit from right which is different in both the numbers
        res = (res ^ (res -1 ))^res;
        
        
        //Seperating numbers into b1 and b2 based on the bit on the res
        for(auto &it : arr)
        {
            if(res & it == 1)
                b1^=it;
            else
                b2^=it;
        }
        res = max(b1,b2);
        output.push_back(res);
        output.push_back(b1+b2-res);
        return output;
    }
};
